---
import { Icon } from "astro-icon/components";
import { technologies } from "../data/technologies";

const whatsappLink = `https://wa.me/+5353118193?text=${encodeURIComponent( "Hola, me interesa trabajar contigo.")}`;
---

<section id="about" class="py-12 md:py-32 bg-gradient-to-b from-white to-gray-50">
  <div class="container mx-auto px-4">
    <!-- Mobile design (card-like layout similar to example A) -->
    <div class="md:hidden">
      <!-- Profile card con nuevo diseño -->
      <div class="bg-white rounded-2xl shadow-lg overflow-hidden mb-8">
        <!-- Contenedor de imagen mejorado -->
        <div class="relative h-48">
          <!-- Fondo decorativo con gradiente -->
          <div class="absolute inset-0 bg-gradient-to-br from-gray-900 to-gray-700"></div>
          
          <!-- Patrón decorativo -->
          <div class="absolute inset-0 opacity-10">
            <svg class="w-full h-full" viewBox="0 0 100 100" preserveAspectRatio="none">
              <pattern id="pattern-grid" x="0" y="0" width="10" height="10" patternUnits="userSpaceOnUse">
                <path d="M 10 0 L 0 0 0 10" fill="none" stroke="currentColor" stroke-width="0.5"/>
              </pattern>
              <rect x="0" y="0" width="100" height="100" fill="url(#pattern-grid)"/>
            </svg>
          </div>

          <!-- Contenedor circular para la imagen -->
          <div class="absolute left-1/2 bottom-0 transform -translate-x-1/2 translate-y-1/2">
            <div class="relative">
              <!-- Anillo decorativo exterior -->
              <div class="absolute -inset-1 bg-white rounded-full"></div>
              <!-- Imagen de perfil -->
              <img
                src="/profile-pic/profile_pic_optimized.webp"
                alt="Jason Guerra"
                class="relative w-24 h-24 rounded-full object-cover border-4 border-white shadow-lg"
                width="96"
                height="96"
                loading="eager"
                fetchpriority="high"
                decoding="async"
              />
            </div>
          </div>
        </div>

        <!-- Contenido del perfil con ajuste de espaciado -->
        <div class="pt-16 px-6 pb-6">
          <div class="flex flex-col items-center">
            <!-- Nombre y título con mejor espaciado -->
            <h2 class="text-2xl font-bold text-gray-900 mb-2">
              Jason Guerra
            </h2>
            
            <div class="flex items-center gap-2 mb-4">
              <span class="bg-gray-100 text-gray-700 px-4 py-1.5 rounded-full text-sm font-medium">
                Desarrollador frontend
              </span>
            </div>

            <!-- Stats con mejor diseño -->
            <div class="w-full grid grid-cols-2 gap-4 mb-6 bg-gray-50 p-4 rounded-xl">
              <div class="text-center">
                <span class="block text-lg font-bold text-gray-900">+3</span>
                <span class="text-sm text-gray-600">Años exp.</span>
              </div>
              <div class="text-center">
                <span class="block text-lg font-bold text-gray-900">10+</span>
                <span class="text-sm text-gray-600">Proyectos</span>
              </div>
            </div>

            <!-- Botón CTA mejorado -->
            <a
              href={whatsappLink}
              target="_blank"
              rel="noopener noreferrer"
              class="w-full flex items-center justify-center px-4 py-3.5 bg-gray-900 text-white rounded-xl hover:bg-gray-800 transition-all transform hover:scale-[0.98] active:scale-[0.97]"
            >
              <Icon name="mdi:whatsapp" class="w-5 h-5 mr-2" />
              Contáctame
            </a>
          </div>
        </div>
      </div>

      <!-- Tab navigation mejorado -->
      <nav class="mb-6 sticky top-0 z-20 bg-white/80 backdrop-blur-md p-2 rounded-xl shadow-sm">
        <div class="grid grid-cols-2 gap-2">
          <button 
            id="tab-about-btn" 
            class="tab-btn flex items-center justify-center gap-2 px-4 py-2.5 rounded-lg w-full"
            aria-selected="true"
            role="tab"
          >
            <Icon name="mdi:account" class="w-5 h-5" />
            <span class="font-medium">Sobre mí</span>
          </button>
          <button 
            id="tab-skills-btn" 
            class="tab-btn flex items-center justify-center gap-2 px-4 py-2.5 rounded-lg w-full"
            aria-selected="false"
            role="tab"
          >
            <Icon name="mdi:code-tags" class="w-5 h-5" />
            <span class="font-medium">Tecnologías</span>
          </button>
        </div>
      </nav>
      
      <!-- Content sections with transitions -->
      <div id="tab-content-container" class="relative">
        <div 
          id="tab-content-about" 
          class="tab-content bg-white rounded-2xl shadow-lg p-6 mb-8 transition-opacity duration-300 ease-in-out"
          role="tabpanel"
        >
          <div class="space-y-5">
            <div>
              <h3 class="text-xl font-semibold mb-3 text-gray-900 flex items-center">
                <Icon name="mdi:account-details" class="w-5 h-5 mr-2 text-gray-700" />
                Sobre mí
              </h3>
              <p class="text-gray-600 text-base leading-relaxed mb-4">
                Mi formación como Ingeniero Industrial me ha proporcionado una sólida base en la gestión de proyectos, la resolución creativa de problemas y las comunicaciones estratégicas.
              </p>
            </div>
            
            <div>
              <h4 class="text-lg font-semibold mb-2 text-gray-900 flex items-center">
                <Icon name="mdi:check-decagram" class="w-5 h-5 mr-2 text-gray-700" />
                ¿Por qué escogerme?
              </h4>
              <p class="text-gray-600 text-base leading-relaxed">
                Estoy comprometido con la calidad, la atención al detalle y el cumplimiento de plazos. Cada proyecto es una oportunidad para demostrar mi profesionalismo y dedicación.
              </p>
            </div>
          </div>
        </div>
        
        <div 
          id="tab-content-skills" 
          class="tab-content bg-white rounded-2xl shadow-lg p-6 mb-8 transition-opacity duration-300 ease-in-out absolute top-0 left-0 w-full opacity-0 pointer-events-none"
          role="tabpanel"
        >
          <h3 class="text-xl font-semibold mb-5 text-gray-900 flex items-center">
            <Icon name="mdi:tools" class="w-5 h-5 mr-2 text-gray-700" />
            Tecnologías
          </h3>
          <div class="grid grid-cols-3 gap-4">
            {technologies.map(tech => (
              <div class="flex flex-col items-center group hover:-translate-y-1 transition-transform" style="contain: paint layout;">
                <div class="p-3 rounded-xl bg-gray-50 shadow-sm mb-2 group-hover:shadow-md transition-shadow w-full flex justify-center">
                  <Icon name={tech.icon} width={28} height={28} style={{color: tech.color}} />
                </div>
                <span class="text-xs text-gray-600 text-center font-medium">{tech.name}</span>
              </div>
            ))}
          </div>
        </div>
      </div>
    </div>
    
    <!-- Desktop design (original layout with minor improvements) -->
    <div class="hidden md:grid md:grid-cols-2 gap-12 items-center">
      <div class="space-y-6">
        <div class="relative animate-fade-up" style="contain: layout style paint;">
          <div class="absolute -inset-1 bg-gradient-to-r from-gray-200 to-gray-100 rounded-full blur opacity-75"></div>
          <img
            src="/profile-pic/profile_pic_optimized.webp"
            alt="Jason Guerra"
            class="relative w-64 h-64 rounded-full mx-auto md:mx-0 object-cover border-4 border-white shadow-lg"
            width="300"
            height="300"
            loading="eager"
            fetchpriority="high"
            decoding="async"
            style="content-visibility: auto;"
          />
        </div>
        <div class="text-center md:text-left animate-fade-up animate-delay-100">
          <a
            href={whatsappLink}
            target="_blank"
            rel="noopener noreferrer"
            class="inline-flex items-center px-8 py-3 bg-gray-900 text-white rounded-full hover:bg-gray-800 transition-colors font-medium tracking-wide"
          >
            <Icon name="mdi:whatsapp" class="w-5 h-5 mr-2" />
            Contáctame
          </a>
        </div>
      </div>

      <div class="space-y-8 animate-fade-up animate-delay-200">
        <div>
          <h2 class="text-4xl md:text-5xl font-bold mb-6 text-gray-900" style="font-display: swap; font-family: system-ui, -apple-system, sans-serif;">
            ¡Hola! Soy<br/><span class="text-gray-800">Jason Guerra</span>
          </h2>
          <p class="text-gray-600 text-lg leading-relaxed">
            Un desarrollador frontend freelance, residente en Miramar, La Habana, Cuba. Mi formación como Ingeniero Industrial me ha proporcionado una sólida base en la gestión de proyectos, la resolución creativa de problemas y las comunicaciones estratégicas.
          </p>
            <h3 class="text-xl font-semibold mt-6 mb-1 text-gray-900">¿Por qué escogerme?</h3>
          <p class="text-gray-600 text-lg leading-relaxed">
            Estoy comprometido con la calidad, la atención al detalle y el cumplimiento de plazos. Cada proyecto es una oportunidad para demostrar mi profesionalismo y dedicación.
          </p>
        </div>

        <div>
          <h3 class="text-xl font-semibold mb-4 text-gray-900">Tecnologías</h3>
          <div class="flex flex-wrap gap-6">
            {technologies.map(tech => (
              <div class="group flex flex-col items-center space-y-2 hover:-translate-y-1 transition-transform" style="contain: paint layout; content-visibility: auto;">
                <div class="p-3 rounded-xl bg-white shadow-md hover:shadow-lg transition-shadow">
                  <Icon name={tech.icon} width={32} height={32} style={{color: tech.color}} />
                </div>
                <span class="text-sm text-gray-600 font-medium">{tech.name}</span>
              </div>
            ))}
          </div>
        </div>
      </div>
    </div>
  </div>
</section>

<style>
  .animate-pulse-slow {
    animation: pulse 3s cubic-bezier(0.4, 0, 0.6, 1) infinite;
  }
  
  .animate-gradient {
    animation: gradient 8s ease infinite;
    background-size: 200% 200%;
  }
  
  .animate-fade-in {
    animation: fadeIn 0.8s ease-out forwards;
  }
  
  @keyframes pulse {
    0%, 100% {
      opacity: 1;
    }
    50% {
      opacity: 0.8;
    }
  }
  
  @keyframes gradient {
    0% {
      background-position: 0% 50%;
    }
    50% {
      background-position: 100% 50%;
    }
    100% {
      background-position: 0% 50%;
    }
  }
  
  @keyframes fadeIn {
    from {
      opacity: 0;
      transform: translateY(10px);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }
  
  .tab-btn {
    color: #6b7280;
    transition: all 0.2s ease;
  }
  
  .tab-btn[aria-selected="true"] {
    color: #111827;
    background-color: #f3f4f6;
    font-weight: 600;
  }
  
  .tab-content {
    transition: opacity 0.3s ease;
  }

  /* Añadir nuevos estilos para animaciones sutiles */
  @keyframes float {
    0%, 100% {
      transform: translateY(0);
    }
    50% {
      transform: translateY(-5px);
    }
  }

  .animate-float {
    animation: float 6s ease-in-out infinite;
  }
</style>

<script>
  function initTabs() {
    const tabAboutBtn = document.querySelector('#tab-about-btn');
    const tabSkillsBtn = document.querySelector('#tab-skills-btn');
    const tabAboutContent = document.querySelector('#tab-content-about');
    const tabSkillsContent = document.querySelector('#tab-content-skills');

    if (!tabAboutBtn || !tabSkillsBtn || !tabAboutContent || !tabSkillsContent) {
      console.error('Elementos de tab no encontrados');
      return;
    }

    const switchTab = (activeBtn: Element, activeContent: Element, inactiveBtn: Element, inactiveContent: Element) => {
      // Actualizar botones
      activeBtn.setAttribute('aria-selected', 'true');
      inactiveBtn.setAttribute('aria-selected', 'false');
      
      // Animar contenido
      inactiveContent.classList.add('opacity-0', 'pointer-events-none');
      activeContent.classList.remove('opacity-0', 'pointer-events-none');
    };

    tabAboutBtn.addEventListener('click', () => {
      switchTab(tabAboutBtn, tabAboutContent, tabSkillsBtn, tabSkillsContent);
    });

    tabSkillsBtn.addEventListener('click', () => {
      switchTab(tabSkillsBtn, tabSkillsContent, tabAboutBtn, tabAboutContent);
    });
  }

  // Asegurarse de que el DOM esté cargado
  document.addEventListener('astro:page-load', initTabs);
  
  // Fallback para cuando astro:page-load no se dispare
  if (document.readyState === 'loading') {
    document.addEventListener('DOMContentLoaded', initTabs);
  } else {
    initTabs();
  }
</script>