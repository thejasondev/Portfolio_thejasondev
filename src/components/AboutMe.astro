---
import { Icon } from "astro-icon/components";
import { technologies } from "../data/technologies";

const whatsappLink = `https://wa.me/+5353118193?text=${encodeURIComponent( "Hola, me interesa trabajar contigo.")}`;
---

<section id="about" class="py-12 md:py-32 bg-gradient-to-b from-white to-gray-50">
  <div class="container mx-auto px-4">
    <!-- Mobile design (modern card layout with improved UX) -->
    <div class="md:hidden">
      <!-- Hero section with profile -->
      <div class="relative mb-8 overflow-hidden">
        <div class="absolute top-0 left-0 w-full h-full bg-gradient-to-b from-gray-900/5 to-gray-900/20 z-0"></div>
        <div class="relative z-10 flex flex-col items-center pt-6 pb-8">
          <!-- Profile image with animated glow effect -->
          <div class="relative mb-5 animate-pulse-slow" style="contain: layout style paint;">
            <div class="absolute -inset-1 bg-gradient-to-r from-gray-200 via-gray-100 to-gray-200 rounded-full blur opacity-75 animate-gradient"></div>
            <img
              src="/profile-pic/profile_pic_optimized.webp"
              alt="Jason Guerra"
              class="relative w-36 h-36 rounded-full object-cover border-4 border-white shadow-xl"
              width="144"
              height="144"
              loading="eager"
              fetchpriority="high"
              decoding="async"
            />
          </div>
          
          <!-- Name with modern typography -->
          <h2 class="text-3xl font-bold text-gray-900 text-center mb-2" style="font-display: swap; font-family: system-ui, -apple-system, sans-serif;">
            Jason Guerra
          </h2>
          
          <!-- Badge with subtle animation -->
          <div class="flex items-center justify-center mb-4 animate-fade-in">
            <span class="bg-gradient-to-r from-gray-800 to-gray-700 text-white px-4 py-1.5 rounded-full text-sm font-medium shadow-sm">
              Frontend Developer
            </span>
          </div>
          
          <!-- Location with icon -->
          <div class="flex items-center justify-center mb-5 text-gray-600">
            <Icon name="mdi:map-marker" class="w-4 h-4 mr-1 text-gray-500" />
            <p class="text-sm">
              Miramar, La Habana, Cuba
            </p>
          </div>
          
          <!-- Highlights section instead of stats -->
          <div class="w-full max-w-xs mb-6 grid grid-cols-2 gap-4">
            <div class="bg-white rounded-xl p-3 shadow-sm flex flex-col items-center justify-center">
              <div class="flex items-center mb-1">
                <Icon name="mdi:clock-outline" class="w-5 h-5 text-blue-400" />
                <span class="ml-1 text-lg font-semibold">+3</span>
              </div>
              <span class="text-xs text-gray-500 uppercase tracking-wide">Años exp.</span>
            </div>
            
            <div class="bg-white rounded-xl p-3 shadow-sm flex flex-col items-center justify-center">
              <div class="flex items-center mb-1">
                <Icon name="mdi:rocket-launch-outline" class="w-5 h-5 text-indigo-500" />
                <span class="ml-1 text-lg font-semibold">10+</span>
              </div>
              <span class="text-xs text-gray-500 uppercase tracking-wide">Proyectos</span>
            </div>
          </div>
          
          <!-- CTA Button with enhanced design -->
          <a
            href={whatsappLink}
            target="_blank"
            rel="noopener noreferrer"
            class="w-full max-w-xs flex items-center justify-center px-6 py-3.5 bg-gradient-to-r from-gray-900 to-gray-800 text-white rounded-full hover:from-gray-800 hover:to-gray-700 transition-all duration-300 font-medium tracking-wide shadow-lg hover:shadow-xl transform hover:-translate-y-0.5"
          >
            <Icon name="mdi:whatsapp" class="w-5 h-5 mr-2" />
            Contáctame
          </a>
        </div>
      </div>
      
      <!-- Tab navigation for content sections -->
      <div class="mb-6 sticky top-0 z-20 bg-white/80 backdrop-blur-md px-2 py-3 rounded-xl shadow-sm">
        <div class="flex justify-around items-center">
          <button id="tab-about-btn" class="tab-btn active flex flex-col items-center px-4 py-1.5 rounded-lg">
            <Icon name="mdi:account" class="w-5 h-5 mb-1" />
            <span class="text-xs font-medium">Sobre mí</span>
          </button>
          <button id="tab-skills-btn" class="tab-btn flex flex-col items-center px-4 py-1.5 rounded-lg">
            <Icon name="mdi:code-tags" class="w-5 h-5 mb-1" />
            <span class="text-xs font-medium">Tecnologías</span>
          </button>
        </div>
      </div>
      
      <!-- Content sections -->
      <div id="tab-content-about" class="tab-content block bg-white rounded-2xl shadow-lg p-6 mb-8">
        <div class="space-y-5">
          <div>
            <h3 class="text-xl font-semibold mb-3 text-gray-900 flex items-center">
              <Icon name="mdi:account-details" class="w-5 h-5 mr-2 text-gray-700" />
              Sobre mí
            </h3>
            <p class="text-gray-600 text-base leading-relaxed mb-4">
              Mi formación como Ingeniero Industrial me ha proporcionado una sólida base en la gestión de proyectos, la resolución creativa de problemas y las comunicaciones estratégicas.
            </p>
          </div>
          
          <div>
            <h4 class="text-lg font-semibold mb-2 text-gray-900 flex items-center">
              <Icon name="mdi:check-decagram" class="w-5 h-5 mr-2 text-gray-700" />
              ¿Por qué escogerme?
            </h4>
            <p class="text-gray-600 text-base leading-relaxed">
              Estoy comprometido con la calidad, la atención al detalle y el cumplimiento de plazos. Cada proyecto es una oportunidad para demostrar mi profesionalismo y dedicación.
            </p>
          </div>
        </div>
      </div>
      
      <div id="tab-content-skills" class="tab-content hidden bg-white rounded-2xl shadow-lg p-6 mb-8">
        <h3 class="text-xl font-semibold mb-5 text-gray-900 flex items-center">
          <Icon name="mdi:tools" class="w-5 h-5 mr-2 text-gray-700" />
          Tecnologías
        </h3>
        <div class="grid grid-cols-3 gap-4">
          {technologies.map(tech => (
            <div class="flex flex-col items-center group hover:-translate-y-1 transition-transform" style="contain: paint layout;">
              <div class="p-3 rounded-xl bg-gray-50 shadow-sm mb-2 group-hover:shadow-md transition-shadow w-full flex justify-center">
                <Icon name={tech.icon} width={28} height={28} style={{color: tech.color}} />
              </div>
              <span class="text-xs text-gray-600 text-center font-medium">{tech.name}</span>
            </div>
          ))}
        </div>
      </div>
    </div>
    
    <!-- Desktop design (original layout with minor improvements) -->
    <div class="hidden md:grid md:grid-cols-2 gap-12 items-center">
      <div class="space-y-6">
        <div class="relative animate-fade-up" style="contain: layout style paint;">
          <div class="absolute -inset-1 bg-gradient-to-r from-gray-200 to-gray-100 rounded-full blur opacity-75"></div>
          <img
            src="/profile-pic/profile_pic_optimized.webp"
            alt="Jason Guerra"
            class="relative w-64 h-64 rounded-full mx-auto md:mx-0 object-cover border-4 border-white shadow-lg"
            width="300"
            height="300"
            loading="eager"
            fetchpriority="high"
            decoding="async"
            style="content-visibility: auto;"
          />
        </div>
        <div class="text-center md:text-left animate-fade-up animate-delay-100">
          <a
            href={whatsappLink}
            target="_blank"
            rel="noopener noreferrer"
            class="inline-flex items-center px-8 py-3 bg-gray-900 text-white rounded-full hover:bg-gray-800 transition-colors font-medium tracking-wide"
          >
            <Icon name="mdi:whatsapp" class="w-5 h-5 mr-2" />
            Contáctame
          </a>
        </div>
      </div>

      <div class="space-y-8 animate-fade-up animate-delay-200">
        <div>
          <h2 class="text-4xl md:text-5xl font-bold mb-6 text-gray-900" style="font-display: swap; font-family: system-ui, -apple-system, sans-serif;">
            ¡Hola! Soy<br/><span class="text-gray-800">Jason Guerra</span>
          </h2>
          <p class="text-gray-600 text-lg leading-relaxed">
            Un desarrollador frontend freelance, residente en Miramar, La Habana, Cuba. Mi formación como Ingeniero Industrial me ha proporcionado una sólida base en la gestión de proyectos, la resolución creativa de problemas y las comunicaciones estratégicas.
          </p>
            <h3 class="text-xl font-semibold mt-6 mb-1 text-gray-900">¿Por qué escogerme?</h3>
          <p class="text-gray-600 text-lg leading-relaxed">
            Estoy comprometido con la calidad, la atención al detalle y el cumplimiento de plazos. Cada proyecto es una oportunidad para demostrar mi profesionalismo y dedicación.
          </p>
        </div>

        <div>
          <h3 class="text-xl font-semibold mb-4 text-gray-900">Tecnologías</h3>
          <div class="flex flex-wrap gap-6">
            {technologies.map(tech => (
              <div class="group flex flex-col items-center space-y-2 hover:-translate-y-1 transition-transform" style="contain: paint layout; content-visibility: auto;">
                <div class="p-3 rounded-xl bg-white shadow-md hover:shadow-lg transition-shadow">
                  <Icon name={tech.icon} width={32} height={32} style={{color: tech.color}} />
                </div>
                <span class="text-sm text-gray-600 font-medium">{tech.name}</span>
              </div>
            ))}
          </div>
        </div>
      </div>
    </div>
  </div>
</section>

<style>
  .animate-pulse-slow {
    animation: pulse 3s cubic-bezier(0.4, 0, 0.6, 1) infinite;
  }
  
  .animate-gradient {
    animation: gradient 8s ease infinite;
    background-size: 200% 200%;
  }
  
  .animate-fade-in {
    animation: fadeIn 0.8s ease-out forwards;
  }
  
  @keyframes pulse {
    0%, 100% {
      opacity: 1;
    }
    50% {
      opacity: 0.8;
    }
  }
  
  @keyframes gradient {
    0% {
      background-position: 0% 50%;
    }
    50% {
      background-position: 100% 50%;
    }
    100% {
      background-position: 0% 50%;
    }
  }
  
  @keyframes fadeIn {
    from {
      opacity: 0;
      transform: translateY(10px);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }
  
  .tab-btn {
    color: #6b7280;
    transition: all 0.3s ease;
  }
  
  .tab-btn.active {
    color: #111827;
    background-color: #f3f4f6;
  }
  
  .tab-content {
    transition: all 0.3s ease;
  }
</style>

<script>
  // Tab functionality with improved logic
  document.addEventListener('astro:page-load', () => {
    const tabAboutBtn = document.getElementById('tab-about-btn');
    const tabSkillsBtn = document.getElementById('tab-skills-btn');
    const contentAbout = document.getElementById('tab-content-about');
    const contentSkills = document.getElementById('tab-content-skills');
    
    function showAboutTab() {
      tabAboutBtn?.classList.add('active');
      tabSkillsBtn?.classList.remove('active');
      contentAbout?.classList.remove('hidden');
      contentAbout?.classList.add('block');
      contentSkills?.classList.add('hidden');
      contentSkills?.classList.remove('block');
    }
    
    function showSkillsTab() {
      tabSkillsBtn?.classList.add('active');
      tabAboutBtn?.classList.remove('active');
      contentSkills?.classList.remove('hidden');
      contentSkills?.classList.add('block');
      contentAbout?.classList.add('hidden');
      contentAbout?.classList.remove('block');
    }
    
    // Initialize with default tab
    showAboutTab();
    
    // Add event listeners
    tabAboutBtn?.addEventListener('click', showAboutTab);
    tabSkillsBtn?.addEventListener('click', showSkillsTab);
  });
</script>